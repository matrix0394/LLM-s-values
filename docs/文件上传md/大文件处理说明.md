# 大文件处理说明

## 概述
本文档列出项目中所有超过GitHub限制（100MB）的大文件，以及处理建议。

## 极大文件（> 100MB）- 必须排除

### 1. 原始数据文件
这些是源数据，不应该上传到GitHub：

| 文件路径 | 大小 | 说明 | 处理方式 |
|---------|------|------|---------|
| `data/country_values/ivs_df.pkl` | 4.2G | IVS原始数据 | **不上传** - 在README中说明数据来源 |
| `data/backup/ivs_df_backup.pkl` | 4.2G | 备份文件 | **不上传** - 备份文件 |
| `data/backup/backup_numpy_problem_files/ivs_df.pkl` | 4.2G | 备份文件 | **不上传** - 备份文件 |
| `data/backup/backup_interviewbuild/country_values/ivs_df.pkl` | 4.2G | 备份文件 | **不上传** - 备份文件 |
| `data/raw/Integrated_values_surveys_1981-2022.sav` | 819M | IVS SPSS原始数据 | **不上传** - 在README中说明获取方式 |
| `data/backup/backup_interviewbuild/raw/Integrated_values_surveys_1981-2022.sav` | 819M | 备份文件 | **不上传** - 备份文件 |
| `data/country_values/valid_data.json` | 114M | 处理后的数据 | **不上传** - 可以通过脚本重新生成 |
| `data/backup/backup_interviewbuild/country_values/valid_data.json` | 114M | 备份文件 | **不上传** - 备份文件 |

### 2. 中间结果文件（PCA等）
这些是分析过程中的中间结果，可以通过运行代码重新生成：

| 文件路径 | 大小 | 说明 | 处理方式 |
|---------|------|------|---------|
| `data/roleplay_English/roleplay_pca_results_*.pkl` | 35-72M | PCA分析结果 | **不上传** - 可重新生成 |
| `data/roleplay_English/llm_roleplay_processed_responses_*.pkl` | 38M | LLM处理后的响应 | **不上传** - 中间结果 |
| `data/country_values/pca_results*.pkl` | 27-40M | PCA结果 | **不上传** - 可重新生成 |
| `data/llm_values/llm_pca_results.pkl` | 33M | LLM PCA结果 | **不上传** - 可重新生成 |
| `data/country_values/valid_data.pkl` | 24M | 处理后的数据 | **不上传** - 可重新生成 |
| `results/country_values/cultural_analysis_results.pkl` | 15M | 分析结果 | **不上传** - 最终结果应导出为轻量格式 |

## 大文件（1-100MB）- 建议排除

### 可视化HTML文件（3-4MB）
- `results/roleplay_English/*.html` 和 `results/roleplay_multilingual/*.html`
- **处理方式**: 这些HTML文件包含大量内嵌数据，建议**不上传**或只上传最新版本

### PNG图片（1-4MB）
- `results/`下的各种PNG图片
- **处理方式**: 可以选择性上传最重要的几张，或者全部上传（PNG可以压缩）

### JSON数据文件（1-3MB）
- `data/roleplay_multilingual/interview_data_comprehensive_*.json`
- `data/roleplay_English/9_25/llm_responses_roleplay/*.json`
- **处理方式**: 可以上传最新版本，删除旧版本

## 推荐的文件分类策略

### ✅ 应该上传到GitHub的文件：
1. **源代码**: 所有`.py`文件
2. **配置文件**: `config/`目录下的小JSON文件
3. **文档**: 所有`.md`文件
4. **需求文件**: `requirements.txt`
5. **小型示例数据**: 用于演示的小样本数据

### ❌ 不应该上传到GitHub的文件：
1. **原始数据文件**: `.pkl`, `.sav`大文件
2. **备份文件**: `data/backup/`整个目录
3. **缓存文件**: `__pycache__/`
4. **中间结果**: 可重新生成的分析结果
5. **临时文件**: 带时间戳的重复文件

### 📝 需要在README中说明的内容：
1. 如何获取IVS原始数据（提供官方链接）
2. 如何运行数据预处理脚本生成中间文件
3. 项目数据文件的组织结构
4. 如何重现实验结果

## 数据管理建议

### 选项1: 使用Git LFS（推荐用于需要版本控制的中等大小文件）
- 适用于10-100MB的重要数据文件
- 需要额外配置和可能的费用

### 选项2: 外部数据存储（推荐用于超大文件）
- 将大文件存储在其他地方（如Google Drive、OSF、Zenodo）
- 在README中提供下载链接和说明

### 选项3: 完全排除（推荐用于可重现的结果）
- 只上传代码和配置
- 提供数据生成脚本
- 用户可以自己运行脚本生成数据

## 建议的.gitignore配置

见下一步创建的`.gitignore`文件。

