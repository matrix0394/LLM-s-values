# 项目大文件总结与处理方案

## 📊 大文件统计

### 超大文件（> 100MB，GitHub禁止上传）

共发现 **8个超大文件**，总计约 **10GB+**：

1. **ivs_df.pkl** - 4.2GB × 4个副本 = **16.8GB**
   - `data/country_values/ivs_df.pkl`
   - `data/backup/ivs_df_backup.pkl`
   - `data/backup/backup_numpy_problem_files/ivs_df.pkl`
   - `data/backup/backup_interviewbuild/country_values/ivs_df.pkl`

2. **Integrated_values_surveys_1981-2022.sav** - 819MB × 2个副本 = **1.6GB**
   - `data/raw/Integrated_values_surveys_1981-2022.sav`
   - `data/backup/backup_interviewbuild/raw/Integrated_values_surveys_1981-2022.sav`

3. **valid_data.json** - 114MB × 2个副本 = **228MB**
   - `data/country_values/valid_data.json`
   - `data/backup/backup_interviewbuild/country_values/valid_data.json`

### 大文件（10-100MB）

共发现 **60+个大文件**，主要包括：

- **PCA结果文件** (35-72MB)：约30个文件
- **处理后的响应数据** (38MB)：约20个文件  
- **其他分析结果** (15-40MB)：约10个文件

### 中等大小文件（1-10MB）

- **HTML可视化文件** (3-4MB)：约100个文件
- **PNG图片** (1-2MB)：约20个文件
- **JSON数据** (1-3MB)：约10个文件

## ✅ 处理方案

### 方案总结

| 文件类型 | 处理方式 | 原因 |
|---------|---------|------|
| **原始数据** (ivs_df.pkl, .sav) | ❌ **不上传** | 文件太大，应在README中说明数据来源 |
| **备份文件** (backup/) | ❌ **不上传** | 重复文件，本地保存即可 |
| **中间结果** (pca_results*.pkl) | ❌ **不上传** | 可通过代码重新生成 |
| **带时间戳的旧文件** | ❌ **不上传** | 只保留最新版本 |
| **HTML可视化文件** | ⚠️ **选择性上传** | 可只上传最重要的几个 |
| **源代码** (.py) | ✅ **必须上传** | 项目核心 |
| **配置文件** (config/*.json) | ✅ **必须上传** | 运行必需 |
| **文档** (.md) | ✅ **必须上传** | 项目说明 |
| **小型结果文件** (.csv, 小JSON) | ✅ **可以上传** | 展示结果 |

## 🚀 快速开始

### 方式1: 使用自动化脚本（推荐）

```bash
# 运行准备脚本
bash 快速更新命令.sh

# 按照脚本提示，确认后执行：
git commit -m "更新项目：添加多语言分析和数据处理优化"
git push -u origin update-20241017
```

### 方式2: 手动执行

```bash
# 1. 进入项目目录
cd "/Users/yxy/code/LLM's values"

# 2. 创建新分支
git checkout -b update-20241017

# 3. 清理Git缓存（重要！移除已跟踪的大文件）
git rm -r --cached .

# 4. 重新添加文件（会自动应用.gitignore规则）
git add .

# 5. 查看状态
git status

# 6. 提交
git commit -m "更新项目：添加多语言分析和数据处理优化"

# 7. 推送到GitHub
git push -u origin update-20241017
```

## 📝 需要在README中添加的说明

建议在主README.md中添加以下章节：

### 数据说明

```markdown
## 数据获取

本项目使用World Values Survey (WVS)和European Values Study (EVS)的整合数据集。

### 原始数据下载

1. 访问官方网站：https://www.worldvaluessurvey.org/
2. 下载文件：`Integrated_values_surveys_1981-2022.sav`
3. 将文件放置在 `data/raw/` 目录下

### 数据预处理

下载原始数据后，运行以下脚本生成所需的中间文件：

```bash
# 1. 数据预处理
python scripts/maintenance/preprocess_data.py

# 2. 生成PCA结果
python scripts/analysis/run_pca_analysis.py
```

### 文件结构

- `data/raw/` - 原始数据文件（不包含在Git中，需自行下载）
- `data/country_values/` - 处理后的国家数据（可通过脚本生成）
- `data/llm_values/` - LLM分析结果
- `results/` - 最终分析结果和可视化
```

## 🔧 .gitignore已配置

已创建的`.gitignore`文件会自动排除：

✅ 所有大于100MB的数据文件  
✅ 备份目录  
✅ 中间结果文件  
✅ 缓存文件  
✅ 带时间戳的临时文件  

但会保留：

✅ 所有源代码  
✅ 配置文件  
✅ 文档文件  
✅ 小型结果文件（JSON, CSV）  

## ⚠️ 注意事项

### 1. 首次推送前的检查

推送前务必检查：

```bash
# 查看将要提交的文件大小
git diff --cached --name-only | xargs du -h | sort -h | tail -20

# 确保没有超过50MB的文件
git ls-files -s | awk '$4 > 50000000 {print $4, $5}'
```

### 2. 如果历史提交包含大文件

如果之前已经提交过大文件到Git历史中，需要清理：

```bash
# 查看历史中的大文件
git rev-list --objects --all | \
  git cat-file --batch-check='%(objecttype) %(objectname) %(objectsize) %(rest)' | \
  sed -n 's/^blob //p' | \
  sort --numeric-sort --key=2 | \
  tail -20

# 使用BFG清理（推荐）
# 下载：https://rtyley.github.io/bfg-repo-cleaner/
java -jar bfg.jar --strip-blobs-bigger-than 50M
git reflog expire --expire=now --all
git gc --prune=now --aggressive
```

### 3. 团队协作建议

如果有团队成员需要完整数据：

1. **选项A**: 提供数据下载链接（Google Drive等）
2. **选项B**: 使用Git LFS（需要配置）
3. **选项C**: 提供数据生成脚本（推荐）

## 📦 推荐的外部存储方案

对于必须分享的大文件：

| 平台 | 优点 | 适用场景 |
|-----|------|---------|
| **Zenodo** | 永久存储，DOI | 学术发布 |
| **OSF** | 版本控制，学术友好 | 研究项目 |
| **Google Drive** | 简单易用 | 团队协作 |
| **Hugging Face Datasets** | 专为数据集设计 | ML数据集 |
| **Git LFS** | 集成Git | 需要版本控制的大文件 |

## 📚 相关文档

- `大文件处理说明.md` - 详细的大文件列表
- `GitHub更新指南.md` - 完整的Git操作指南
- `快速更新命令.sh` - 自动化更新脚本

## ✨ 最终建议

**推荐做法**：

1. ✅ 使用提供的`.gitignore`排除所有大文件
2. ✅ 只上传代码、配置和文档
3. ✅ 在README中说明数据获取方式
4. ✅ 提供数据处理脚本供用户重现结果
5. ✅ 将可重现性作为项目目标

**不推荐**：

1. ❌ 上传原始数据文件到GitHub
2. ❌ 保留大量带时间戳的重复文件
3. ❌ 上传可以重新生成的中间结果
4. ❌ 使用Git LFS存储可重现的分析结果

这样可以保持仓库轻量化，同时确保项目的可重现性！

