# 三阶段实验统一说明 - 5次重复取众数

## 📅 修改日期
2025年10月19日

## 🎯 修改目标
**统一三个阶段的实验方法论**：所有阶段都采用"5次完整问卷重复，每个问题取众数"的策略。

## 🔧 具体修改

### Stage 1: LLM固有价值观 (llm_values)

#### 修改文件
1. `src/llm_values/llm_interview.py`
2. `src/run/run_llm_values_analysis.py`

#### 主要变化
```python
# 修改前
def __init__(self, repeat_count: int = 1):
    super().__init__(repeat_count=repeat_count)

# 修改后
def __init__(self, repeat_count: int = 5):
    super().__init__(repeat_count=1)  # 基类重试次数
    self.consensus_count = repeat_count  # 众数重复次数
```

#### 新增功能
- ✅ 添加 `_calculate_consensus()` 方法：计算众数
- ✅ 添加 `_multi_round_interview()` 方法：多轮访谈
- ✅ 修改 `interview_entity()` 方法：支持多轮模式
- ✅ 更新默认参数：`repeat_count=5`

### Stage 2: 英语角色扮演 (roleplay_English)

#### 修改文件
1. `src/run/run_roleplay_english_analysis.py`

#### 主要变化
```python
# 修改前
consensus_count = 1  # 默认1次

# 修改后  
consensus_count = 5  # 默认5次，保持三阶段一致
```

#### 说明
- ✅ 代码已有众数逻辑（`_multi_round_interview` 和 `_calculate_consensus`）
- ✅ 只需修改默认参数从1改为5
- ✅ 已有的 109 个国家数据**可能需要重跑**

### Stage 3: 多语言对比 (roleplay_multilingual)

#### 修改文件
无需修改

#### 说明
- ✅ 已经实现了完整的5次重复取众数逻辑
- ✅ 配置文件中 `repeat_count=5`
- ✅ 需要重新运行实验（之前12%的数据可能使用了不同参数）

## 📊 实验设计统一

### 重复策略
所有三个阶段现在使用**相同的方法**：

```
1. 完整问卷第1轮 → 10个问题的回答
2. 完整问卷第2轮 → 10个问题的回答
3. 完整问卷第3轮 → 10个问题的回答
4. 完整问卷第4轮 → 10个问题的回答
5. 完整问卷第5轮 → 10个问题的回答
   ↓
6. 计算每个问题的众数
   - A008: [3,3,2,3,3] → 众数=3 (置信度80%)
   - A165: [2,2,2,2,2] → 众数=2 (置信度100%)
   - ...
```

### 温度设置
所有三个阶段使用**固定温度**：

```python
temperature = 0.1  # 所有尝试都相同
```

不再使用动态温度（0.1 → 0.2 → 0.3...）

### 重试机制
```python
max_retries = 5  # API失败或格式错误时重试
temperature = 0.1  # 重试时温度不变
```

## 🎓 学术意义

### 1. 方法论一致性
- 三个阶段使用完全相同的测量方法
- 消除方法差异对结果的影响
- 增强跨阶段比较的有效性

### 2. 提高可靠性
- 5次重复捕捉模型的真实倾向
- 众数减少随机波动的影响
- 置信度指标量化结果稳定性

### 3. 可重复性
- 明确的实验参数
- 标准化的数据收集流程
- 便于其他研究者验证

### 4. 控制严格性
- 固定温度消除混淆变量
- 重复策略标准化
- 符合科学实验设计原则

## 📈 实验规模估算

### Stage 1: LLM固有价值观
```
7个模型 × 10个问题 × 5次重复 = 350次API调用
预计时间: 约30-45分钟
预计费用: $0.20-0.30
```

### Stage 2: 英语角色扮演
```
7个模型 × 109个国家 × 10个问题 × 5次重复 = 38,150次API调用
预计时间: 并发10 → 约6-8小时
预计费用: $20-25
```

### Stage 3: 多语言对比
```
7个模型 × 54个语言-国家组合 × 10个问题 × 5次重复 = 18,900次API调用  
预计时间: 并发15 → 约4-6小时
预计费用: $11-15
```

### 总计
```
总API调用: 57,400次
总时间: 约12-16小时（并发执行）
总费用: $31-40
```

## ⚠️ 重要提醒

### 已有数据需要重跑

1. **Stage 1 (LLM Values)**
   - 如果之前只跑了1次，需要重跑为5次
   
2. **Stage 2 (Roleplay English)**  
   - 如果之前的109国家数据是1次访谈，需要重跑为5次
   - 数据位置：`data/roleplay_English/9_25/`
   
3. **Stage 3 (Roleplay Multilingual)**
   - 已经跑了12%的数据可能使用了不同参数
   - 建议重新开始，确保一致性

### 数据识别

新旧数据可通过以下方式区分：
- **文件名时间戳**：新数据文件有更新的时间戳
- **数据内容**：新数据中每个回答包含 `consensus` 或 `confidence` 信息
- **文件路径**：可以创建新的子目录如 `10_19/` 存放新数据

## 🚀 运行命令

### Stage 1
```bash
python3 src/run/run_llm_values_analysis.py
# 自动使用 repeat_count=5
```

### Stage 2
```bash
python3 src/run/run_roleplay_english_analysis.py
# 选择模式，自动提示 consensus_count=5
```

### Stage 3
```bash
python3 src/run/run_roleplay_multilingual_analysis.py
# 选择 "全面测试"，自动使用 repeat_count=5
```

## 📝 论文写作建议

在方法论部分可以这样描述：

> **数据收集程序**
> 
> 为确保测量的可靠性和三个阶段的可比性，我们采用了统一的数据收集策略：
> 
> 1. **重复测量**：对每个实验条件（模型-国家-语言组合），我们进行5次完整问卷访谈。每次访谈包含全部10个价值观问题。
> 
> 2. **众数聚合**：对于每个问题，我们从5次重复中计算众数作为最终答案。这种方法既能捕捉模型的主导倾向，又能通过置信度指标（众数出现频率）量化回答的稳定性。
> 
> 3. **温度控制**：所有API调用使用固定温度参数（temperature=0.1），确保结果的可重复性。重试机制（最多5次）用于处理API错误或格式问题，但温度在所有尝试中保持不变。
> 
> 4. **数据质量**：这种设计使我们能够：(a) 识别模型回答的不确定性，(b) 确保跨阶段测量的标准化，(c) 提供结果稳定性的量化指标。

## ✅ 修改验证清单

- [x] Stage 1 代码修改完成
- [x] Stage 2 代码修改完成  
- [x] Stage 3 确认已实现
- [x] 温度固定为0.1
- [x] 默认参数统一为5
- [x] 文档更新完成
- [ ] Stage 1 数据重跑
- [ ] Stage 2 数据重跑
- [ ] Stage 3 数据重跑
- [ ] 论文方法论更新

---

**修改人**: AI Assistant  
**审核人**: 待用户确认  
**状态**: ✅ 代码修改完成，等待数据重跑

